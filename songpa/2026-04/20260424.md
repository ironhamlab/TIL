* 단원명 : 웹 표준 프레임워크
* 학습 주제 : Next.js

---


#### [학습 내용]
## 5. Router
### 5-5. Zustand
#### 5-5-2. zustand and query

</br>

**<오늘 배운 것 요약>**
1. **브라우저에서 영구히 기억할 것**: Zustand + persist 사용.

2. **서버(DB)에서 가져와서 보여줄 것**: React Query 사용.

3. **Next.js 에러 방지**: useEffect와 isMounted로 브라우저 로딩 확인 후 렌더링.

4. **효율성**: staleTime을 조절해서 서버에 불필요한 노크(API 요청) 줄이기.

</br>

---
</br>

💡 **Zustand & Persist: "기록 남기기"**

* 새로고침을 해도 남겨둬야 하는 상태값은 브라우저의 저장소인 **'로컬 스토리지'** 에 저장해야 함.
* **Zustand의 `persist` 미들웨어**는 상태값을 로컬스토리지에 자동으로 저장해줌.

</br>

* 하지만 서버는 로컬스토리지가 뭔지 모르기 때문에, 서버가 그린 화면과 클라이언트가 그린 화면이 달라 Next에서 에러 발생할 수 있음.

</br>

* 해결책: `isMounted`라는 상태를 만들어, **브라우저에 완전히 로드된(Mounted) 후에만 화면을 보여주도록 처리**하기 (PersistPage 코드가 이 역할을 함)

</br></br></br>

**<예시 코드>** 

* 테마의 상태값을 지속적으로 저장 -> 새로고침을 해도 다크모드 저장됨

</br>

* @/store/usePersistStore
```js
import { create } from "zustand";
import { persist } from "zustand/middleware";

export const usePersistStore = create(
    persist(    // localStorage 에 저장하는 기능 
        (set) => ({
            theme: "light",
            toggleTheme: () => 
                set((state) => ({theme: state.theme === "light"? "dark":"light"}))
        }),
        {
            name: "theme-stroage"
        }
    )
);
```

</br>

* @/app/persist/page.js
```js
'use client';
import { usePersistStore } from "@/store/usePersistStore";
import { useEffect, useState } from "react";

export default function PersistPage() {

    const { theme, toggleTheme } = usePersistStore();

    // Hydration 에러 해결하기 위한 state 선언
    const [isMounted, setIsMounted] = useState(false);

    useEffect(() => {
        setIsMounted(true);
    }, []);

    if(!isMounted) return <div>로딩 중...</div>;

    return (
        <div style={{ background: theme === "dark"? "#333": "#fff", height:"100vh", padding:"20px"}}>
            <p>현재 테마: {theme}</p>
            <button onClick={toggleTheme}>테마 변경</button>
        </div>
    )
}
```

</br></br></br>

---
</br>

💡 **React Query: "서버 데이터 관리자"**


* `@tanstack/react-query`는 API 통신을 혁신적으로 줄여줌
* **Providers.js**: QueryClientProvider는 **앱 전체에 "데이터 캐시 보관소"** 를 깔아주는 역할. 모든 페이지에서 **useQuery() 를 통해** 서버 데이터를 공유할 수 있게 해줌.
    * **'use client'** 필수 사용!! 반드시 클라이언트 상태여야 함! 

</br>

* **staleTime**: "데이터가 신선하다고 인정해 주는 시간"으로, 설정한 시간 동안은 API를 재호출하지 않고 아까 받은 **데이터를 재사용(캐싱)** 함.

</br>

* **useQuery**: 데이터를 가져올 때(fetchUsers) 로딩 중인지(isLoading), 에러가 났는지(isError)를 자동으로 알려줌

</br>

* 설치 명령어
    ```bash
    $ npm install @tanstack/react-query
    ```

</br></br></br>

**<예시 코드>**

* @/app/Providers.js
```js
'use client'

import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
import { useState } from "react"

export default function Providers({children}) {
    const [queryClient] = useState(() => new QueryClient({
        defaultOptions: {
            queries: {
                staleTime: 60 * 1000,   // 1분동안은 다시 요청하지 않고, 캐시를 사용
            },
        },
    }));

    return (
        <QueryClientProvider client={queryClient}>
            {children}
        </QueryClientProvider>
    );
}
```

</br>

* @/app/query/page.js
```js
'use client';

import { useQuery } from "@tanstack/react-query";
import { fetchUsers } from "../api/userApi";

export default function QueryPage() {
    /**
     * useQuery
     * queryKey: 데이터 고유 이름(캐싱의 키)
     * queryFn: 데이터를 가져올 비동기 함수
     */
    const { data, isLoading, isError, error } = useQuery({
        queryKey:["users"],
        queryFn: fetchUsers,
    });

    // 로딩상태 처리
    if(isLoading) return (
        <h1>데이터를 불러오는 중입니다...</h1>
    );

    // 에러상태 처리
    if(isError) return (
        <p>{error.message}</p>
    );

    // 성공 시 데이터 출력
    return (
        <>
            <h1>유저 목록</h1>
            <ul>
                {data?.map(user => (
                    <li key={user.id}>{user.name}</li>
                ))}
            </ul>
        </>
    );
}
```

</br></br></br>

---
</br>

💡 **JSON-Server: "가짜 백엔드"**
* 백엔드 API 완성되기 전 사용

* **db.json** 파일 하나만 있으면 실제 REST API 서버처럼 동작하게 해주는 도구

* postApi.getPosts() 같은 함수를 호출하면 실제로 db.json에서 데이터를 읽어오거나 써줌.

</br>

* 설치 및 사용 명령어
```bash
# 설치 명령어
$ npm install json-server


# db.json 생성 후 사용
# 변경 사항 자동 감지 옵션 --watch
# 3000 포트와 겹칠 수 있으므로 안쓰는 포트로 매핑
$ npx json-server --watch db.json --port 4000
```

**<예시 코드>**

* @/app/api/PostApi
```js
const BASE_URL = "http://localhost:3001/posts";

export const postApi = {

    // 전체 목록 가져오기
    getPosts: async() => {
        const res = await fetch(BASE_URL);
        return res.json();
    },

    // 새로운 글 등록하기
    // 주소는 그대로지만 method를 POST로!!
    // body에 데이터를 담아 보낸다.
    creatPost: async(newPost) => {
        const res = await fetch(BASE_URL,{
            method: "POST",
            headers: {"Content-Type": "application/json"}, // JSON데이터임을 알리기
            body: JSON.stringify(newPost),  // 객체를 문자열로 변환
        });
        return res.json();
    }
}
```

</br>

* @/app/crud-test/page.js
```js
'use client';
import { useEffect, useState } from "react";
import { postApi } from "../api/PostApi";

export default function CRUDPage() {

    const [posts, setPosts] = useState([]);

    // Read: 처음 로드할 때 목록 가져오기
    const loadPosts = async() => {
        const data = await postApi.getPosts();
        setPosts(data);
    };

    useEffect(() => {loadPosts();}, []);

    // 글 등록 테스트
    const handleAdd = async() => {
        await postApi.creatPost({title: "새로운 글" + Date.now(), author: "익명"});
        loadPosts();
    };

    return (
        <>
            <button onClick={handleAdd}>
                새 글 등록
            </button>

            {posts.map(post => (
                <div key={post.id} style={{ border: '1px solid black'}}>
                    <h3> {post.title} (작성자: {post.author})</h3>
                </div>
            ))}
        </>
    )
}
```

</br></br></br></br>

#### [느낀 내용]

* zustand와 그 미들웨어인 persist로 상태값을 저장해줄 수 있는게 신기했다.
* Providers.js와 zustand의 역할이 아직 헷갈리긴 한데, 캐싱 기능을 넣어줄 수 있고 앱 전체에서 쓰는 데이터 공급소라고 한다. 미니 프로젝트를 통해 실습을 하면서 좀더 와닿을 수 있으면 좋겠다.
* 아직 백엔드 진도를 나가기 전이라 json-server를 유용하게 사용할 수 있을 것 같다.


</br>

#### [활용 방안]

* zustand - persist 사용으로 새로고침해도 상태값 유지하기

* Providers.js로 데이터 캐싱하기

* Json-server로 fake 백엔드 서버 띄워서 비동기처리 등 실습하기